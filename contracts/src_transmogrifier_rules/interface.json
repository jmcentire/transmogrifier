{
  "component_id": "src_transmogrifier_rules",
  "name": "Rule-Based Register Rewriting",
  "description": "Level 2 rule-based register rewriting engine. Applies deterministic regex-based transformations to convert text between registers (casual, academic, narrative, technical → direct). Zero API calls, sub-millisecond performance.",
  "version": 1,
  "types": [
    {
      "name": "RewriteRule",
      "kind": "struct",
      "fields": [
        {
          "name": "pattern",
          "type_ref": "str",
          "required": true,
          "default": "",
          "description": "Regex pattern to match in text",
          "validators": []
        },
        {
          "name": "replacement",
          "type_ref": "str | Callable",
          "required": true,
          "default": "",
          "description": "Replacement string or callable for matched pattern",
          "validators": []
        }
      ],
      "item_type": "",
      "variants": [],
      "inner_types": [],
      "description": "A regex pattern and replacement pair for text transformation",
      "owner_component": ""
    },
    {
      "name": "RuleEngine",
      "kind": "struct",
      "fields": [],
      "item_type": "",
      "variants": [],
      "inner_types": [],
      "description": "Deterministic register rewriter using pattern-based rules. Applies transformation rules <1ms.",
      "owner_component": ""
    }
  ],
  "functions": [
    {
      "name": "rewrite",
      "description": "Rewrite text from source register to target register using predefined regex rules. Returns original text if source equals target or no transformation rules exist. Attempts indirect routing through 'direct' register if no direct path exists.",
      "inputs": [
        {
          "name": "self",
          "type_ref": "RuleEngine",
          "required": true,
          "default": "",
          "description": "RuleEngine instance",
          "validators": []
        },
        {
          "name": "text",
          "type_ref": "str",
          "required": true,
          "default": "",
          "description": "Input text to rewrite",
          "validators": []
        },
        {
          "name": "source",
          "type_ref": "Register | str",
          "required": true,
          "default": "",
          "description": "Source register (enum with .value attribute or string)",
          "validators": []
        },
        {
          "name": "target",
          "type_ref": "Register | str",
          "required": true,
          "default": "",
          "description": "Target register (enum with .value attribute or string)",
          "validators": []
        }
      ],
      "output_type": "str",
      "error_cases": [],
      "preconditions": [
        "text is a valid string",
        "source and target are either strings or objects with a .value attribute"
      ],
      "postconditions": [
        "Returns non-empty string",
        "If source == target, returns original text unchanged",
        "If no rules exist for (source, target), returns text possibly routed through 'direct' register",
        "Result is stripped of leading/trailing whitespace unless strip produces empty string",
        "If strip produces empty string, returns original text"
      ],
      "idempotent": true,
      "is_async": false,
      "side_effects": [],
      "structured_side_effects": [
        {
          "kind": "none",
          "target": "",
          "description": ""
        }
      ],
      "performance_budget": {
        "p95_latency_ms": 1,
        "max_memory_mb": null,
        "complexity": "O(n*m) where n=text length, m=number of rules"
      }
    }
  ],
  "dependencies": [
    "re",
    "dataclasses"
  ],
  "dependency_contracts": {},
  "invariants": [
    "Supported register transformations are hardcoded: (casual→direct), (academic→direct), (narrative→direct), (technical→direct)",
    "All regex substitutions use re.IGNORECASE flag",
    "Rule application is sequential and deterministic within each rule list",
    "Empty stripped results fall back to original text",
    "Callable replacements are treated identically to string replacements in re.sub"
  ],
  "requires": [],
  "processing_register": "direct",
  "data_access": {
    "reads": [],
    "writes": [],
    "rationale": "Pure transformation logic with no persistence",
    "side_effects": []
  },
  "authority": {
    "domains": [],
    "rationale": null
  }
}