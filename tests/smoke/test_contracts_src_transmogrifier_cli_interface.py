"""Smoke tests for contracts/src_transmogrifier_cli/interface.py (auto-generated by pact adopt)."""

import pytest

def test_import_contracts_src_transmogrifier_cli_interface():
    """Module imports without error."""
    import importlib
    mod = importlib.import_module("contracts.src_transmogrifier_cli.interface")
    assert mod is not None

def test_main_is_callable():
    """Function main exists and is callable."""
    import importlib
    mod = importlib.import_module("contracts.src_transmogrifier_cli.interface")
    fn = getattr(mod, "main", None)
    assert fn is not None, "'main' not found in contracts.src_transmogrifier_cli.interface"
    assert callable(fn)

def test_detect_is_callable():
    """Function detect exists and is callable."""
    import importlib
    mod = importlib.import_module("contracts.src_transmogrifier_cli.interface")
    fn = getattr(mod, "detect", None)
    assert fn is not None, "'detect' not found in contracts.src_transmogrifier_cli.interface"
    assert callable(fn)

def test_classify_is_callable():
    """Function classify exists and is callable."""
    import importlib
    mod = importlib.import_module("contracts.src_transmogrifier_cli.interface")
    fn = getattr(mod, "classify", None)
    assert fn is not None, "'classify' not found in contracts.src_transmogrifier_cli.interface"
    assert callable(fn)

def test_translate_is_callable():
    """Function translate exists and is callable."""
    import importlib
    mod = importlib.import_module("contracts.src_transmogrifier_cli.interface")
    fn = getattr(mod, "translate", None)
    assert fn is not None, "'translate' not found in contracts.src_transmogrifier_cli.interface"
    assert callable(fn)

def test_profile_is_callable():
    """Function profile exists and is callable."""
    import importlib
    mod = importlib.import_module("contracts.src_transmogrifier_cli.interface")
    fn = getattr(mod, "profile", None)
    assert fn is not None, "'profile' not found in contracts.src_transmogrifier_cli.interface"
    assert callable(fn)

def test_profile_list_is_callable():
    """Function profile_list exists and is callable."""
    import importlib
    mod = importlib.import_module("contracts.src_transmogrifier_cli.interface")
    fn = getattr(mod, "profile_list", None)
    assert fn is not None, "'profile_list' not found in contracts.src_transmogrifier_cli.interface"
    assert callable(fn)

def test_profile_show_is_callable():
    """Function profile_show exists and is callable."""
    import importlib
    mod = importlib.import_module("contracts.src_transmogrifier_cli.interface")
    fn = getattr(mod, "profile_show", None)
    assert fn is not None, "'profile_show' not found in contracts.src_transmogrifier_cli.interface"
    assert callable(fn)

def test_profile_calibrate_is_callable():
    """Function profile_calibrate exists and is callable."""
    import importlib
    mod = importlib.import_module("contracts.src_transmogrifier_cli.interface")
    fn = getattr(mod, "profile_calibrate", None)
    assert fn is not None, "'profile_calibrate' not found in contracts.src_transmogrifier_cli.interface"
    assert callable(fn)
